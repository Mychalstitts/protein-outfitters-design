# Protein Outfitters — Slow-Crawl Audit v2 (May 4, 2026)

Code-side scan across 34 HTML pages + browser walkthrough of major flows. Severity:
- **P0** = blocks a real user, or actively misleads
- **P1** = broken-but-not-fatal, hardcoded demo data leaking into UX
- **P2** = polish, copy, dead anchors

---

## ✅ FIXED THIS PASS

| File | Fix |
|---|---|
| index.html | "Verdant" → "Protein Outfitters" |
| brand.html | "Verdant" → "Protein Outfitters" (×2) |
| cuts.html | "Verdant" → "Protein Outfitters" |
| site-visit.html | "Verdant" → "Protein Outfitters" (×3); 4 dead anchors → real targets |
| reviews.html | "Lorem ipsum" → realistic review copy |
| producers.html | "Twelve verified producers across..." → "Verified producers across..."; filter chip "All · 12" → "All" |
| farmer.html | hardcoded "Tuesday · May 5" now overridden by JS → today's actual date |
| processor.html | same date override |
| (earlier passes) | account.html greeting + day label dynamic; cumulative all-in price; map rebuild; PO monogram brand thread; image path fixes |

---

## 🔴 STILL BROKEN — needs fix

### Hardcoded fake data on dashboards (P0 — owner asked us to flag this)

These pages still show invented numbers/users/animals as if real:

- **`/farmer`** — "$48k YTD revenue", "847 followers", greeting "You've got 3 reservations…", Today feed with Tyler W. / Sarah R. / #2641 / #2671. Whole page is a Stitch demo, not wired to DB.
- **`/processor`** — "14 in queue · 87% capacity used · $12k this week", Today's queue with #2598/#2702/#2641. Hardcoded.
- **`/finance`** — "$4,287.50 available balance" hardcoded; "$1,395 pending"; "Transfer to bank" / "Schedule auto-transfer" buttons aren't wired.
- **`/admin`** — sidebar counts (Disputes 4, Processors 12, Animals 87, Pool $28k) hardcoded; 4 fake disputes with detailed Stripe IDs.
- **`/reviews`** — Sarah R. / Tyler W. / #2641 / #2671 / #2598 hardcoded reviews. (Some are fine as "example" reviews if labeled, but currently presented as real.)
- **`/list-animal`** — uses "#2641 · Plant 04" placeholders in form preview.

**Right fix:** rewrite each dashboard page to fetch from real API + show empty states for new users. The existing endpoints exist (`/api/listings`, `/api/farms`, `/api/processors`, `/api/reservations`) — these pages just don't query them.

### Tailwind CDN leftovers (P2)

These pages still load `cdn.tailwindcss` (Stitch import that was never integrated): cuts.html, onboarding.html, profile.html, settings.html, site-visit.html, trends.html. Not strictly broken, but big load cost (~300KB) and means brand colors aren't applied. **Fix:** rebuild those pages on po-shell.css like /map and /admin-overview were.

### Dead anchors `href="#"` (P2)

Remaining counts (after this pass): trends.html (11), farmer.html (4), donation-flow.html (1), hardware.html (1), listing.html (1), processor-saas.html (4), reserve-flow.html (2), onboarding.html (2), processor.html (1).

The trends.html ones are mostly nav placeholders for time ranges (Day/Week/Month) that should toggle UI state via JS, not navigate. Most others are nav links that should point to /discover, /map, /producers, /account.

### `placeholder` references (P1 review)

Most are legitimate `<input placeholder="...">` attributes (good). But `processor-pricing.html` has 17 placeholder mentions — likely the "ham/bacon/sausage rates" form skeleton with placeholder copy. Worth a real review.

### Demo listing IDs everywhere (P0)

23+ pages reference `#2641 / #2702 / #2598 / #2671` as if they exist. Real DB has UUIDs like `5b859840-eb4a-4d11-a597-cc31d4802964` for `#118` and `#214`. Anyone clicking these on the demo pages lands on "Listing not found." Fix path: stop seeding pages with demo numbers; pull listings from `/api/listings` so real IDs flow through.

---

## 🟡 NEED EXTERNAL ACTION FROM MYCHAL

| What | Why | Where |
|---|---|---|
| `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` in Vercel | Activate live deposit charges + reservation status updates | dashboard.stripe.com → API keys + webhooks |
| `GOOGLE_MAPS_KEY` in Vercel | Activate the real Google Map at /map (currently shows fallback) | console.cloud.google.com → already created |
| Enable **Geocoding API** + **Places API (New)** on the Google Cloud project | `/api/discover-nearby` (partner search) won't work without these | console.cloud.google.com/apis/library |
| Run migration once: `curl -X POST "https://www.proteinoutfitters.com/api/migrate?secret=$MIGRATE_SECRET"` | Adds new `discovered_partners` + `invites` tables | terminal |
| (Optional) Switch Stripe to test mode by adding `sk_test_` key + creating test products | Safe testing without real card charges | dashboard.stripe.com |

---

## 🟢 FUNCTIONALITY WISHLIST (priority-ranked)

1. **Wire farmer/processor/finance/admin dashboards to real DB** — kill the Stitch demo hardcoding. Highest UX impact.
2. **USDA FSIS bulk import** (~6,000 processors, free Excel) → seed `discovered_partners` nationally.
3. **Admin "Find nearby" widget on /admin-overview** — paste zip, get candidates with one-click invite.
4. **reCAPTCHA Enterprise on /invite-partner** before opening to traffic (Google offers 10k free/mo).
5. **Stripe Connect for marketplace payouts** — currently single-account, all money goes to platform. Connect lets us pay farmer + processor splits automatically.
6. **State + species coverage map** on /admin-overview — see where we have farms/processors and where we don't.
7. **`/discover` location-aware** — currently distance is hardcoded `100 mi`. Wire user's zip from auth → real distance via Geocoding API.
8. **Listing photos** — current listings have empty image areas on /discover cards (img.naturalWidth=0). Either Adobe Stock import or wire photo upload to Vercel Blob (already plumbed).
9. **Replace demo listing references** in static pages with `<script>` that pulls real listings → so #2641 doesn't appear in copy when DB doesn't have it.
10. **Send invites in batches via Cloud Scheduler** — once we have FSIS data loaded, drip 50/day.

---

## TOOLS WE COULD USE

- **Adobe MCP (already connected)** — generate hero photography for empty listing cards (`asset_search` from Adobe Stock or `image_apply_*` for transformations).
- **Gemini (already wired via /api/concierge)** — verify breed names, write personalized invite copy, write listing descriptions.
- **Stripe MCP (connected)** — programmatically reconcile reservations vs payments, refund the cancelled ones, generate disputes view.
- **Trello MCP** — push the still-broken P0/P1 items into Trello cards so we don't lose them.

---

## NEXT 3 THINGS TO SHIP IF YOU GREENLIGHT

1. **Replace farmer/processor/finance/admin demo data with real-DB queries + empty states** (~2 hours of editing, no new infra)
2. **USDA FSIS bulk import + admin discovery widget** (~1 hour)
3. **Set the Maps + Stripe env vars and run migration** (~5 min on your side)

After that, the entire flow is real money + real data + real partner pipeline. Ready to ship when you are.
