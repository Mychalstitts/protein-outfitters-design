# Protein Outfitters — Audit v3 (May 4, 2026)

After dashboard rewiring (commit 68). Comparing to v2.

---

## ✅ FIXED SINCE V2

| Area | Before | After |
|---|---|---|
| Brand name leaks | "Verdant" on 4 pages (incl. homepage) | 0 ✅ |
| Marketing lies | "Twelve verified producers" copy + filter | Removed ✅ |
| Lorem ipsum | Live on /reviews | Replaced ✅ |
| `/farmer` dashboard | Hardcoded "$48k YTD · 847 followers · Tyler W. flag · 4 fake events" | Real today's date, real first name, real farm "Stitty Ranch · Verified producer", real "0 listings · 0 reservations" empty stats, "No active listings yet" empty-state copy ✅ |
| `/processor` dashboard | "$12k this week · 14 in queue · 87% capacity · Plant 04 / 4.8★" | Real date + name, "You haven't claimed a plant yet" empty state with "Set up your plant →" CTA ✅ |
| `/finance` dashboard | "$4,287.50 available · $1,395 pending · $48k YTD" | Live `/api/admin-metrics` → "$0.00" until first real Stripe payment ✅ |
| `/admin` Stitch demo | 4 fake disputes, fake sidebar counts, "Pool ($28k)" | Hard redirect to `/admin-overview` ✅ |
| Hardcoded "Tuesday · May 5" greetings | Top of /farmer + /processor | Now dynamic via `new Date()` ✅ |
| `/site-visit` | 4 dead anchors, "Verdant" brand | Real targets, fixed brand ✅ |

---

## 🔴 CAUGHT THIS PASS — needs fix

### `/admin-overview` 500 error (P0)
- Live page shows `FUNCTION_INVOCATION_FAILED` from `/api/admin-metrics`. Wrapped each query individually + made Stripe import lazy so missing key/package no longer crashes the whole endpoint. Pushed in this commit.
- Likely root cause: ESM import of `stripe` failed before Vercel's npm install picked up the new dep, or our hard-fail Stripe code path threw on missing key. Now resilient.

### `/processor` Activity section (P1)
- Below the empty-state hero, an "Activity" feed was still showing 5 hardcoded entries: "Sarah R. checked in for #2598", "Berkshire hog booking", "Stripe payout $3,200 to Wells Fargo", etc.
- Hidden via `display:none;` until we wire a real activity log. Pushed.

### Hardcoded demo listings still polluting static pages (P1)
- `/index` product cards reference `#2641 / #2702` that don't exist in DB → "Listing not found" if clicked.
- `/checkout`, `/confirmed`, `/cut-sheet`, `/list-animal`, `/listing` static demo HTML mentions `#2641 / Plant 04`.
- `/reviews` has 14+ refs (Sarah R., Tyler W., #2641, etc.) — entire page is a Stitch mockup.
- `/farmer` script-side has 1 ref to `#2641` left over (in a fallback string) — mostly harmless.
- **Right fix:** rewrite `/index` cards to source from `/api/listings`, rewrite `/reviews` to fetch real reviews from `/api/reviews`. ~1 hour each.

### Tailwind CDN leftovers (P2)
Still on: `cuts.html`, `onboarding.html`, `profile.html`, `settings.html`, `site-visit.html`, `trends.html`. Each loads `https://cdn.tailwindcss.com` (~300KB) and uses Stitch-generated classes that don't match brand. **Rebuild on po-shell.css** in batches.

### Dead anchors `href="#"` (P2)
- `trends.html` (11) — time-range pills (Day/Week/Month) that should be JS toggles, not links
- `processor-saas.html` (4)
- `account.html` (5) — modal-open triggers that look like nav
- `finance.html` (4)
- `reserve-flow.html`, `onboarding.html`, `donation-flow.html`, `hardware.html`, `listing.html`, `processor.html`: 1–2 each

### Producers page (P2)
- Footer/sidebar copy still mentions `847` (legacy follower count). Cosmetic.

### `/farmer` "Mychalstitts." capitalization (P2)
- The signed-in user has `name = "Mychalstitts"` (their GitHub login). Greeting reads "Good morning, Mychalstitts." — accurate but not their actual first name. **Fix:** add a profile-edit step where users can correct their display name. (Already supported by the schema, just no UI for it.)

---

## 🔵 STILL PENDING — environment / external

| Action | Why |
|---|---|
| Add `STRIPE_SECRET_KEY` to Vercel | Activates Stripe Checkout flow + populates `/admin-overview` balance |
| Add `STRIPE_WEBHOOK_SECRET` to Vercel | Reservations flip to `deposit-paid` after a real charge |
| Add `GOOGLE_MAPS_KEY` to Vercel | Real map at `/map` (currently graceful fallback message) |
| Run migration: `curl -X POST "https://www.proteinoutfitters.com/api/migrate?secret=$MIGRATE_SECRET"` | Adds `discovered_partners` + `invites` tables for the partner-invite system |

---

## 📊 SCORECARD

| Metric | v2 | v3 | Delta |
|---|---:|---:|---:|
| Pages with hardcoded fake greetings | 5 | 0 | −5 |
| Pages with fake $ balances | 3 | 0 | −3 |
| "Verdant" / "Lorem ipsum" / "Twelve verified" copy artifacts | 7 | 0 | −7 |
| Pages with broken-anchor stitch leftovers | 14 | 11 | −3 |
| Dashboards backed by real DB | 1 (/account) | 4 (+farmer, +processor, +finance, +admin→admin-overview) | +3 |
| Demo listing IDs (#2641 etc) on static pages | 39 | 36 | −3 (mostly script fallbacks now) |

---

## NEXT 3 SHIPS IF YOU GREENLIGHT

1. **Replace `/index` homepage product cards with real listings from `/api/listings`** — kills the dead-link `#2641` problem on the front door.
2. **Rewrite `/reviews` to fetch real reviews + show empty state** — kills 14+ references to fake users in one shot.
3. **Rebuild Tailwind-CDN pages on po-shell.css** — `cuts`, `onboarding`, `profile`, `settings`, `site-visit`, `trends`. Saves ~300KB per page load and gets them on-brand.

After those three, the entire customer-facing surface is on real data. The remaining demo content (processor-saas, processor-pricing, screens.html) is partner-facing or dev-only.
