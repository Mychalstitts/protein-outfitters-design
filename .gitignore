.vercel
node_modules/
.DS_Store

# scratch: mount cannot delete files, moved here instead
_to_delete/

# --- lockfiles -------------------------------------------------------------
# Root lockfile: static-site CI. Mobile lockfile: Mobile CI + EAS (npm ci).
# Other nested lockfiles stay ignored so deploy/ keeps `npm install` on Vercel.
package-lock.json
!/package-lock.json
!/mobile/package-lock.json

# --- mobile / shared (nested under mobile/) -------------------------------
dist/
build/
.expo/
.expo-shared/
*.tsbuildinfo
mobile/apps/mobile/ios/
mobile/apps/mobile/android/
*.xcworkspace
*.xcodeproj

# Env / secrets — NEVER commit
.env
.env.local
.env.*.local
*.pem
*.key
*.keystore
*.p8
*.p12
*.mobileprovision
mobile/play-store-service-account.json

# Logs / editors / coverage
*.log
npm-debug.log*
.vscode/
.idea/
*.swp
coverage/
